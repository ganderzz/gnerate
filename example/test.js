const initialState = {

};

export default function reducer(state = initialState, action) {
    switch(action.type) {
        default:
            return initialState;
    }
}
            Gnerate Version: 0.0.7
                    File Name: test
                    File Extension: js
                </div>

                <div>
                    Additional Params:
                    Param?: 3
                    keysCanBeAnything?: additionalParam
                </div>
            </section>
        );
    }
}
